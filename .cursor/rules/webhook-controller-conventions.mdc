---
description: WebhookForTG 的 Controller 層慣例，包含驗證、回傳格式、同時發送邏輯
globs: Controllers/**/*.cs
alwaysApply: false
---

# Webhook Controller 慣例

## 路由結構
- 基礎路由：`[Route("api/[controller]")]`
- 主要端點：`POST /api/webhook`（JSON Body）
- 簡易端點：`POST /api/webhook/simple`（Query String）

## Secret Key 驗證

Controller 透過 `X-Webhook-Secret` Header 驗證，`SecretKey` 為空時跳過驗證：

```csharp
private bool IsAuthorized()
{
    if (string.IsNullOrWhiteSpace(_secretKey)) return true;
    Request.Headers.TryGetValue("X-Webhook-Secret", out var headerSecret);
    return headerSecret == _secretKey;
}
```

新增端點時，第一行必須呼叫 `IsAuthorized()`。

## 同時發送多平台

使用 `Task.WhenAll` 同時發送，不可序列等待：

```csharp
// ✅ 正確 - 並行發送
var lineTask = _lineService.SendMessageAsync(msg);
var telegramTask = _telegramService.SendMessageAsync(msg);
await Task.WhenAll(lineTask, telegramTask);

// ❌ 錯誤 - 序列發送
await _lineService.SendMessageAsync(msg);
await _telegramService.SendMessageAsync(msg);
```

## HTTP 回傳規則

| 情境 | Status Code |
|------|------------|
| 全部平台成功 | `200 OK` |
| 部分平台失敗 | `207 Multi-Status` |
| 訊息為空 | `400 Bad Request` |
| Secret 驗證失敗 | `401 Unauthorized` |

## 回傳格式
回傳 `NotifyResult` 物件，包含每個平台的 `Success` 與 `Error`。
