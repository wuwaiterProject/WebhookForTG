---
description: WebhookForTG 專案的 C# .NET 8 通用慣例，包含命名規則、DI 注入、設定讀取等
alwaysApply: true
---

# .NET 8 專案通用慣例

## 命名規則
- 介面前綴 `I`，例如 `ILineNotifyService`
- 非同步方法加後綴 `Async`，例如 `SendMessageAsync`
- 設定類別對應 `appsettings.json` 的 Section 名稱

## 相依性注入
- 所有服務透過建構子注入，不使用 Service Locator
- `HttpClient` 透過 `IHttpClientFactory`（即 `AddHttpClient<TInterface, TImpl>()`）注入
- 建構子參數若來自 `IConfiguration`，遇到必填值用 `??` 拋 `InvalidOperationException`

```csharp
// ✅ 正確
_token = configuration["Section:Key"]
    ?? throw new InvalidOperationException("Section:Key is not configured.");

// ❌ 錯誤
_token = configuration["Section:Key"]!;
```

## 錯誤處理
- 服務層用 tuple `(bool Success, string? Error)` 回傳結果，不拋例外給 Controller
- Controller 層依結果回傳對應 HTTP status code
  - 全部成功 → `200 OK`
  - 部分失敗 → `207 Multi-Status`
  - 驗證失敗 → `400 Bad Request`
  - 未授權 → `401 Unauthorized`

## 設定檔
- 敏感資訊（Token、Secret）只放 `appsettings.json`，不 hardcode 在程式碼
- 不在 `appsettings.Development.json` 放真實的 Token
